# System packages required for Streamlit Cloud deployment
# These packages will be installed via apt-get

# For PDF generation (ReportLab)
libfreetype6-dev
libfontconfig1-dev

# For image processing (if needed)
libjpeg-dev
libpng-dev

# For system monitoring (psutil)
gcc
python3-dev

# For database operations
sqlite3

# For better performance
libssl-dev
libffi-dev